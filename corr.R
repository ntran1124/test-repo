corr <- function(directory, threshold = 0) {
        correlation <- vector(mode="numeric", length=0) ## create an empty numeric vector for correlations
        
        ## your code will be here

        return(correlation)
}
